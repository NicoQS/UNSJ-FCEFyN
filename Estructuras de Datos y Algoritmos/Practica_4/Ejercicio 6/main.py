from Simulador import Simulador


if __name__ == "__main__":
    Simulador_ = Simulador()
    Simulador_.simular()
    print("Fin de la simulacion")
    
    
    
    
    
    
    
    
    
    
    
    #Prueba de Heap
    """monticulo = ColadePrioridad(15)
    monticulo.insertar(10)
    monticulo.insertar(15)
    monticulo.insertar(5)
    monticulo.insertar(20)
    monticulo.insertar(25)
    monticulo.insertar(30)
    monticulo.insertar(4)
    monticulo.mostrar()
    print("El valor minimo es: " + str(monticulo.eliminar()))
    monticulo.mostrar()
    print("El valor minimo es: " + str(monticulo.eliminar()))
    monticulo.mostrar()
    print("El valor minimo es: " + str(monticulo.eliminar()))
    monticulo.mostrar()"""